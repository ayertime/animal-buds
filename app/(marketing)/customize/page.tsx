import CustomizeClient from "@/components/customize-client";

export const metadata = {
  title: "Customize Your Bud — Animal Buds",
  description: "Pick a name and dress them up.",
};

export default function CustomizePage() {
  return <CustomizeClient />;
}
